# CLAUDE.md — Claude Code Instructions

Follow the shared repository instructions in `AGENTS.md`.

Claude Code-specific note: this repository is a plugin marketplace at the root, but the installable plugin source is `plugins/job-harness`. Do not recreate commands, skills, scripts, MCP config, or Python source at the repository root.
