interface:
  display_name: "Job Search Workflow"
  short_description: "Run the full Job Harness search workflow"
  brand_color: "#0F766E"
  default_prompt: "Use $job-search-workflow to find jobs matching my brief."
policy:
  allow_implicit_invocation: true
